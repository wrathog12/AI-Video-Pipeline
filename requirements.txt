# Pinned for determinism (see context.md §7). Upgrade deliberately, never incidentally.

# --- Phase 0: walking skeleton ---
pydantic==2.12.3          # IR schema validation
PyYAML==6.0.3             # config.yaml
edge-tts==7.2.8           # TTSProvider: synthesis + native WordBoundary timings
numpy==2.3.3              # PCM assembly in the sample domain
soundfile==0.13.1         # WAV decode/encode (libsndfile)

# --- LLM annotation ---
google-genai==1.74.0      # default annotator: Gemini structured outputs + seed
requests==2.32.5          # Cartesia TTS streams over SSE; no SDK needed

# --- Optional alternates (R7). Not on the default path. ---
# anthropic>=0.75.0       # providers.llm: claude-*  (ClaudeAnnotator)
# num2words==0.5.14       # numeral expansion, only needed by the whisperx aligner
# whisperx                # providers.aligner: whisperx  (pulls torch, ~2.5GB)
# pytesseract             # qa.ocr_gate: true  (also needs the tesseract binary)
#
# The pipeline runs end to end with nothing from this block installed: it falls
# back to the heuristic annotator, native alignment, and no OCR gate.
